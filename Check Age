function checkAge(name, age) {
  if ((age >= 21)) {
	return('Welcome, ' + name + '!');
 
  }  else {
  	return('Go home, ' + name + '!');
  }
}
var output = checkAge('Adrin', 22);
console.log(output);
